package ru.skzmk.backend.general.data.handler.auth;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@RequiredArgsConstructor
@Service
public class AuthLoginService {

//    public RequestActionResult<AuthResponse, ?> authorization(LoginRequest request) {
//        RequestActionResult<AuthResponse, ?> resultValidation = validationData(request);
//        if (resultValidation != null) return resultValidation;
//    }
//    private RequestActionResult<AuthResponse, ?> validationData(LoginRequest request) {
//
//    }
}
